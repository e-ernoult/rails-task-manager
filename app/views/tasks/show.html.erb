<h1><%= @task.title %></h1>

<button class="btn"><%= link_to "Edit task", task_edit_path(@task.id) %></button>

<p><%= @task.details %></p>

<p><%= @task.completed ? "[x] This task is completed." : "[ ] This task is not completed yet." %> </p>

<%= link_to "Back to list", tasks_path %>
